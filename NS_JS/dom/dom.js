// BOX-Model in CSS
// DOM model in JS
// text node is the last point in the node

var items = document.getElementsByClassName('list-group-item');
items[1].style.backgroundColor = 'grey';
console.log(items[1].textContent);